#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * @argc: argument count
 * @argv: array of argument
 * Return: always 0
 */

int main(int argc, char *argv[])
{
argv[1] = 0;
printf("%d", argc);
return (0);
}