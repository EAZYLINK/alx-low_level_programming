#include "main.h"

/**
 * print_sign - prints the sign of a given character
 * @n: parameter to be tested
 * Return: either 1, 0 or -1 based on sign of parameter
 */

int print_sign(int n)
{
if (n > 0)
_putchar('+');
return (1);
else if (n = 0)
_putchar('0');
return (0);
else
_putchar('-');
return (-1);
}
