#include "main.h"

/**
 * _abs - prints the absolute value of an int
 * @int: integer parameter
 * Return: always positive
 */

int _abs(int)
{
if (int < 0)
{
int = int * -1;
return(int);
}
else
{
return(int);
}
}
