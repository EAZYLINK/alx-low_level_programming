#include "main.h"

/**
 * _abs - prints the absolute value of an int
 * @n: integer parameter
 * Return: always positive
 */

int _abs(int n)
{
if (n < 0)
{
n = n * -1;
return (n);
}
else
{
return (n);
}
}
