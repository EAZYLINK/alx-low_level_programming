#include "main.h"

/**
 * negative_or_positive - tests if a parameter is negatice or positive
 * @1: parameter to be tested
 * Return: 0
 */

int main(void)
{
int i = 0;
positive_or_negative(i);
return (0);
}
